我要做一个 AI 财务报销信息抽取练习项目，目标是练习 Prompt 工程。

技术栈：Node.js + Express。

请先不要直接写代码，先给我项目结构和实现计划。

项目目标：
实现 POST /extract-reimbursement 接口。

输入：
{
  "text": "我5月1日去上海出差，住宿和交通一共报销1280元，但发票金额是1200元。"
}

输出必须是合法 JSON：
{
  "报销类型": string | null,
  "金额": number | null,
  "日期": string | null,
  "风险点": string[]
}

规则：
1. 报销类型只能根据文本判断，不能胡编。
2. 金额必须是数字。
3. 日期尽量标准化成 YYYY-MM-DD。
4. 如果字段无法判断，返回 null。
5. 风险点是数组，没有风险则返回 []。
6. 接口第一版可以先用 mock 逻辑实现，后面再接大模型 API。
7. 你先给计划，等我确认后再创建文件。q


